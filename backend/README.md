# Backend Setup

- Env: create a `.env` file in this folder with:
```
MONGO_URI=mongodb://127.0.0.1:27017/lostfound
PORT=5000
```
- Install: `npm install`
- Run (dev): `npm run dev`

Requires a running MongoDB instance.

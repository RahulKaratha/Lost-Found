{
  "name": "lost-found-portal",
  "private": true,
  "version": "1.0.0",
  "description": "Complete Lost & Found Portal with Admin Management",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "concurrently \"npm --prefix backend start\" \"npm --prefix frontend preview\"",
    "setup": "npm run install:all && npm --prefix backend run seed:admin",
    "build": "npm --prefix frontend run build"
  },
  "keywords": ["lost-and-found", "portal", "react", "express", "mongodb"],
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}


